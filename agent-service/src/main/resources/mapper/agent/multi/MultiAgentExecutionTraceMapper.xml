<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huah.ai.platform.agent.multi.MultiAgentExecutionTraceMapper">
    <select id="selectByTraceIdAndUserId" resultType="com.huah.ai.platform.agent.multi.MultiAgentExecutionTrace">
        SELECT * FROM ai_multi_agent_traces
        WHERE trace_id = #{traceId}
          AND user_id = #{userId}
        LIMIT 1
    </select>
    <select id="selectRecentByUser" resultType="com.huah.ai.platform.agent.multi.MultiAgentExecutionTrace">
        SELECT * FROM ai_multi_agent_traces
        WHERE user_id = #{userId}
        <if test="sessionId != null and sessionId != ''">
            AND session_id = #{sessionId}
        </if>
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>
    <select id="selectRecentAfter" resultType="com.huah.ai.platform.agent.multi.MultiAgentExecutionTrace">
        SELECT * FROM ai_multi_agent_traces
        WHERE created_at &gt;= #{after}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>
    <select id="countByAgentType" resultType="long">
        SELECT COUNT(*) FROM ai_multi_agent_traces WHERE agent_type = #{agentType}
    </select>
    <select id="countByAgentTypeBefore" resultType="long">
        SELECT COUNT(*) FROM ai_multi_agent_traces
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
    </select>
    <select id="selectArchiveCandidates" resultType="com.huah.ai.platform.agent.multi.MultiAgentExecutionTrace">
        SELECT * FROM ai_multi_agent_traces
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
        ORDER BY created_at ASC
        LIMIT #{limit}
    </select>
    <select id="selectArchiveCandidatesBatch" resultType="com.huah.ai.platform.agent.multi.MultiAgentExecutionTrace">
        SELECT * FROM ai_multi_agent_traces
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
        ORDER BY created_at ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>
    <delete id="deleteByAgentTypeBefore">
        DELETE FROM ai_multi_agent_traces
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
    </delete>
</mapper>
