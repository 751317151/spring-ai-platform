<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huah.ai.platform.agent.audit.AiToolAuditLogMapper">
    <select id="selectRecent" resultType="com.huah.ai.platform.agent.audit.AiToolAuditLogEntity">
        SELECT * FROM ai_tool_audit_logs
        WHERE 1 = 1
        <if test="userId != null and userId != ''">
            AND user_id = #{userId}
        </if>
        <if test="agentType != null and agentType != ''">
            AND agent_type = #{agentType}
        </if>
        <if test="toolName != null and toolName != ''">
            AND tool_name = #{toolName}
        </if>
        <if test="traceId != null and traceId != ''">
            AND trace_id = #{traceId}
        </if>
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>
    <select id="selectRecentByAgentTypeAfter" resultType="com.huah.ai.platform.agent.audit.AiToolAuditLogEntity">
        SELECT * FROM ai_tool_audit_logs
        WHERE agent_type = #{agentType}
          AND created_at &gt;= #{after}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>
    <select id="countByAgentType" resultType="long">
        SELECT COUNT(*) FROM ai_tool_audit_logs WHERE agent_type = #{agentType}
    </select>
    <select id="countByAgentTypeBefore" resultType="long">
        SELECT COUNT(*) FROM ai_tool_audit_logs
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
    </select>
    <select id="selectArchiveCandidates" resultType="com.huah.ai.platform.agent.audit.AiToolAuditLogEntity">
        SELECT * FROM ai_tool_audit_logs
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
        ORDER BY created_at ASC
        LIMIT #{limit}
    </select>
    <select id="selectArchiveCandidatesBatch" resultType="com.huah.ai.platform.agent.audit.AiToolAuditLogEntity">
        SELECT * FROM ai_tool_audit_logs
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
        ORDER BY created_at ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>
    <delete id="deleteByAgentTypeBefore">
        DELETE FROM ai_tool_audit_logs
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
    </delete>
</mapper>
