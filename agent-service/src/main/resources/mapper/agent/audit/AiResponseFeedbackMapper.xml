<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huah.ai.platform.agent.audit.AiResponseFeedbackMapper">
    <select id="selectByResponseId" resultType="com.huah.ai.platform.agent.audit.AiResponseFeedback">
        SELECT * FROM ai_response_feedback WHERE response_id = #{responseId}
    </select>
</mapper>
