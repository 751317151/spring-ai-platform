<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huah.ai.platform.agent.audit.AiAuditLogMapper">
    <select id="selectByUserIdOrderByCreatedAtDesc" resultType="com.huah.ai.platform.agent.audit.AiAuditLog">
        SELECT * FROM ai_audit_logs
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>
    <select id="selectByAgentTypeAfter" resultType="com.huah.ai.platform.agent.audit.AiAuditLog">
        SELECT * FROM ai_audit_logs
        WHERE agent_type = #{agentType}
          AND created_at &gt; #{after}
        ORDER BY created_at DESC
    </select>
    <select id="statsGroupByAgent" resultType="map">
        SELECT agent_type AS agentType, COUNT(*) AS cnt, AVG(latency_ms) AS avgLatency
        FROM ai_audit_logs
        WHERE created_at &gt; #{since}
        GROUP BY agent_type
    </select>
    <select id="topTokenUsersSince" resultType="map">
        SELECT user_id AS userId,
               SUM(COALESCE(prompt_tokens, 0) + COALESCE(completion_tokens, 0)) AS totalTokens
        FROM ai_audit_logs
        WHERE created_at &gt; #{since}
        GROUP BY user_id
        ORDER BY totalTokens DESC
    </select>
    <select id="countByUserIdAfter" resultType="long">
        SELECT COUNT(*) FROM ai_audit_logs
        WHERE user_id = #{userId}
          AND created_at &gt; #{after}
    </select>
    <select id="countByAgentType" resultType="long">
        SELECT COUNT(*) FROM ai_audit_logs WHERE agent_type = #{agentType}
    </select>
    <select id="countByAgentTypeBefore" resultType="long">
        SELECT COUNT(*) FROM ai_audit_logs
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
    </select>
    <select id="selectArchiveCandidates" resultType="com.huah.ai.platform.agent.audit.AiAuditLog">
        SELECT * FROM ai_audit_logs
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
        ORDER BY created_at ASC
        LIMIT #{limit}
    </select>
    <select id="selectArchiveCandidatesBatch" resultType="com.huah.ai.platform.agent.audit.AiAuditLog">
        SELECT * FROM ai_audit_logs
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
        ORDER BY created_at ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>
    <delete id="deleteByAgentTypeBefore">
        DELETE FROM ai_audit_logs
        WHERE agent_type = #{agentType}
          AND created_at &lt; #{before}
    </delete>
</mapper>
