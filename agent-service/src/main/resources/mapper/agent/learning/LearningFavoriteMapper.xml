<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huah.ai.platform.agent.learning.LearningFavoriteMapper">
    <select id="selectByUserId" resultType="com.huah.ai.platform.agent.learning.LearningFavoriteEntity">
        SELECT * FROM learning_favorites
        WHERE user_id = #{userId}
        ORDER BY COALESCE(last_collected_at, created_at) DESC
    </select>
    <delete id="deleteByUserIdAndId">
        DELETE FROM learning_favorites
        WHERE user_id = #{userId}
          AND id = #{id}
    </delete>
</mapper>
