<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huah.ai.platform.common.persistence.audit.AiEvidenceFeedbackMapper">
    <select id="selectByResponseIdAndChunkId" resultType="com.huah.ai.platform.common.persistence.audit.AiEvidenceFeedbackEntity">
        SELECT * FROM ai_evidence_feedback
        WHERE response_id = #{responseId}
          AND chunk_id = #{chunkId}
    </select>
    <select id="countStatsByKnowledgeBaseId" resultType="map">
        SELECT
            COUNT(*) AS total,
            COUNT(*) FILTER (WHERE feedback = 'up') AS positive,
            COUNT(*) FILTER (WHERE feedback = 'down') AS negative
        FROM ai_evidence_feedback
        <if test="knowledgeBaseId != null">
            WHERE knowledge_base_id = #{knowledgeBaseId}
        </if>
    </select>
</mapper>
